<div class= "container">
<h2>ログイン</h2>
<h5>会員の方はこちらからログイン</h5>
<%= form_with model:@customer, url: customer_session_path, method: :post,local:true do |f| %>

  <table class="table table-borderless">
    <tbody>
      <tr>
        <th class="col-2">メールアドレス</th>
        <td><%= f.email_field :email, autofocus: true, autocomplete: "email",class: "justify-content-start" %></td>
      </tr>
      <tr>
        <th class="col-2">パスワード</th>
        <td><%= f.password_field :password, autocomplete: "current-password" %></td>
      </tr>
      <tr>
        <th class="col-2"></th>
        <td><%= f.submit "ログイン" ,class: 'btn btn-primary' %></td>
      </tr>
    </tbody>
  </table>
<% end %>

<h5>会員登録がお済みでない方</h5>
 <p><%= link_to 'こちら', new_customer_registration_path %>から新規登録を行ってください。</p>
</div>